# Standalone Nginx configuration for Host OS installation (/etc/nginx/sites-available/eoffice.adminead.com)
# This is an alternative to the Dockerized `nginx` service (see docker-compose.yml / DEPLOYMENT.md),
# for when Nginx runs on the host instead of in a container. The frontend container no longer
# publishes a host port by default (it's proxied to internally on the Docker network), so using
# this file requires adding a "ports: - 5180:80" mapping to the frontend service first.
#
# Usage:
# 1. sudo cp nginx/eoffice.adminead.com.conf /etc/nginx/sites-available/eoffice.adminead.com
# 2. sudo ln -s /etc/nginx/sites-available/eoffice.adminead.com /etc/nginx/sites-enabled/
# 3. sudo certbot --nginx -d eoffice.adminead.com

upstream erp_backend {
    server 127.0.0.1:3001;
}

upstream erp_frontend {
    server 127.0.0.1:5180;
}

server {
    listen 80;
    listen [::]:80;
    server_name eoffice.adminead.com;

    # Allow Let's Encrypt ACME challenges
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # API Proxy
    location /api/ {
        proxy_pass http://erp_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Frontend Proxy
    location / {
        proxy_pass http://erp_frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
